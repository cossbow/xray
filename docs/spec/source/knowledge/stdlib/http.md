---
module: http
summary: HTTP client and server (REST API, routing, middleware)
---
# http module

HTTP client and server (REST API, routing, middleware).

Usage: `import http` then call `http.function()`.
